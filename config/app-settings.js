
'use strict';

/**
 * appSettings - all relative to the project root
 */
const appSettings = {
    cloudant_db_name: 'order',
    access_control_db_name: 'access_control',
    cloudant_max_items: 200000,
    cloudant_bulk_batch_size: 10000,
};

module.exports = appSettings;
